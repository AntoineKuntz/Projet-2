{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Horaire</h1>
    <a href="/disponibility/add">Ajouter un Horaire</a> <br> <br>
    {% for disponibility in disponibilitys %}
        <li>Nom Horaire : {{ disponibility.timetable }}<a href="/Disponibility/edit/{{ disponibility.id }}">Editer vos horaires</a>
        <form action="/Disponibility/delete/{{ disponibility.id }}" method="POST">
            <button>Supprimer</button>
        </form>
        </li><br>

    {% endfor %}
{% endblock %}